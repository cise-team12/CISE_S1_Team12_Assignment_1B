language: php

php:
 - 5.5
 - 5.6
 - 7.0
 - hhvm
 
before_script:
 - composer self-update
install:
 - composer install
 
script: phpunit

matrix:
 allow_failures:
   - php: 5.6
   - php: hhvm
 fast_finish: true

services:
 - mysql
